# How to upgrade

Upgrade the `opendkim-operator` charm with the `refresh` command:

```bash
juju refresh opendkim-operator
```

Check that the `opendkim-operator` charm is active and running:

```bash
juju status
```
